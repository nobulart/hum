# SURFACE.md

The morning transfer layer. When `DREAMS.md` is read at surfacing time (~07:00),
fragments that clear the significance threshold land here — temporarily in the
conscious stream, ranked by `total_weight`, until they are either acted on
(filed to `DREAMS_DAY.md`, `SUBCONSCIOUS.md`, or `SOUL.md`) or fade away without trace.

Each entry keeps the YAML front matter it carried from `DREAMS.md` and gains surfacing
metadata (`surfaced_at`, `total_weight`, `counterdream`). Nothing here is accepted as
true merely because it surfaced.

## Surfaced fragments
